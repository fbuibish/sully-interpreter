const detectAction = (text: string) => {
    const result = text;

    // use llm to detect if there is an action

    // format data

    // save in db -> run through business logic

    return result;
};

export default detectAction;
